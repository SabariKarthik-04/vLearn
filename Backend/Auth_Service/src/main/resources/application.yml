spring:
  application:
    name: Auth-Service
  datasource:
    url: jdbc:mysql://localhost:3306/vlearn_auth_db
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081
  servlet:
    context-path: /auth
    

  
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/